# Jules — Round 2

Content & community marketer; I batch social graphics all day and live to drop a stack of
photos and get them all back cut out on a brand color. Re-tested the updated app cold on
desktop: dropped 3 photos at once, let them finish, set Color + Drop shadow, used Download
all (ZIP). THIS TIME I FOUND THE BATCH/ZIP FLOW.

## Prior concerns (R1) — re-checked
1. "No batch ZIP / Download-all" — FIXED. Dropped 3 photos in one go; all 3 finished in the
   queue and a "Download all (ZIP) — 3 photos, Square 1080×1080" button appeared. The ZIP
   downloaded and contained 3 real, separate 1080×1080 JPEGs, named a-/b-/c-square-1080x1080.
2. "Settings don't apply across the batch" — FIXED. Export bg/size/Drop shadow are sticky for
   the whole queue — all 3 ZIP files came out at the same size with the shadow baked in. The
   ZIP button itself names the shared size, so I know what I'm getting for all of them.
3. "First-photo ~50MB cold-start wait" — ADDRESSED in copy. The dropzone now says "First photo
   downloads a one-time ~50 MB tool — next photos take ~10 seconds each," so it's no longer a
   nasty surprise. (In my run it was quick, but the expectation-setting is the right fix.)

## 1. CLARITY — Yes
Same strong cold open, now even clearer for me: "Drag & drop photos (up to 20)" and "Drop up
to 20 photos at once — they're processed one by one" tell a batch user immediately this is for
volume. H1 + "free, no upload, no signup" + the on-device badge still nail it in <10s.

## 2. VALUE — Yes
Today: remove.bg (paywalls volume) + Photoroom (charges for the shadow) + manual per-platform
re-export. Here I dropped 3, set one brand bg + Drop shadow once, and got a ZIP of all three at
1080×1080 with the shadow correctly baked. That is literally the "drop 15, get a ZIP back"
afternoon-saver I said was missing in R1. Replaces two paid tools for my #1 task.

## 3. ADVOCACY — 9/10
The thing standing between "nice" and "I'd post about this" is shipped: real batch + Download
all (ZIP) + shared settings. I'd bring this up unprompted in a marketing Slack. Not a 10 only
because the cutout quality is model-bound on hairy/complex subjects (a known ceiling) and I'd
want a quick per-photo size override for the rare odd-one-out — but the core promise now lands.

```json
{"tester": 6, "round": 2, "clarity": "Yes", "value": "Yes", "advocacy": 9, "topComplaints": ["Cutout quality model-bound on complex/hairy edges (acceptable ceiling)", "No quick per-photo size override when one image in a batch needs a different size"], "priorConcernsAddressed": "all"}
```
