# Dana — Round 2

**Prior concerns re-checked first:**
- (R1 #2) Two confusable "shadow" controls → **FIXED.** They now read literally "Remove cast shadow — Cleans the dark shadow under your subject" (a cleanup checkbox up top) and "Drop shadow" (a toggle that ADDS a shadow), and Drop shadow moved into the export row beside Background + Margin. I knew which was which on sight, no pause. Exported shadow-ON vs shadow-OFF JPGs confirm the toggle adds a real soft drop shadow (file grew 11KB→13.3KB, shadow visible under the product).
- (R1 #1) Marketplace-first framing → **partly improved.** The subline now reads "Headshots & avatars · ads & social · slides & mockups · marketplace listings" — marketplace is dead last, ads & social is second. The eBay/Etsy/Poshmark preset ROW still sits in the controls (marketplace presets), but it no longer leads the value pitch. Good enough that I no longer thought "not for me."

**1. CLARITY — Yes.** Headline "Remove any background — keep your photo on this device" + "white, your brand color, or transparent — free, no upload, no signup" still told me the whole job in one read. The White/Color/Transparent tiles with "ads & social" under Color speak straight to me.

**2. VALUE — Yes.** Same killer flow as R1, re-run clean: dropped a product photo, got a cutout, hit the export "Color" tab, typed my hex into the field labeled `#RRGGBB` (#1A73E8 stuck), picked Link/Ad 1200×627, exported. Output was exactly 1200×627 on my brand blue, filename `product-linkad-1200x627-1a73e8.jpg` — preset + hex encoded. That's the Canva round-trip I redo every campaign, gone. With Drop shadow now unambiguous I'll actually use it for ads.

**3. ADVOCACY — 9/10.** Bumped from 8. The shadow naming that made me pause last round is fully resolved, and the marketer-vs-reseller framing no longer trips me — those were my two gaps. I'd screenshot this into the team channel unprompted today. Not a 10 only because the export "Color" tab + hex field live below the fold under a dense control stack; first time I had to scroll and hunt for where to set the brand color (I clicked the top preview "Color" tile by reflex, which is just a sample). A "set brand color" affordance nearer the cutout would make it a 10.

## What still holds back the 10
- Brand-color entry is below the fold under the cutout; easy to confuse the top preview tile with the export Color tab on first use.
- Marketplace preset row still present in controls (fine, just not my use case).

```json
{"tester": 5, "round": 2, "clarity": "Yes", "value": "Yes", "advocacy": 9, "topComplaints": ["Brand-color/hex entry sits below the fold; easy to confuse the top preview 'Color' tile with the export Color tab on first use", "Marketplace preset row still occupies controls space"], "priorConcernsAddressed": "all"}
```
